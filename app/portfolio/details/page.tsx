import { PortfolioDetailsLayout } from "./portfolio-details-layout"

/** Categorized project work (tabs) below the same hero band as `/portfolio` (aligned column + header). */
export default function PortfolioDetailsPage() {
  return (
    <main className="min-h-screen bg-background">
      <PortfolioDetailsLayout />
    </main>
  )
}
